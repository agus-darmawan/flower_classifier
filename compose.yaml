version: "3.9"
name: flask
services:
  flask:
    restart: unless-stopped
    build:
      context: ./flask
      dockerfile: Dockerfile
    deploy:
      resources:
        reservations:
          cpus: "1.0"
          memory: 512M
    ports:
      - mode: ingress
        target: 5001
        published: 5001
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5001/"]
